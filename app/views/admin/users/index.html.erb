
<main>
  <h1> Welcome to the Users Page </h1>


  <div class="well">
    <%= link_to 'Register New User', new_user_path, class: 'btn btn-info' %>
  </div>

  <table>
    <thead>
      <tr>
        <th>First Name</th>
        <th>Last Name</th>
        <th>Email</th>
        <th>Delete</th>
      </tr>
    </thead>
    <tbody>
      <% @users.each do |user| %>
        <tr>
          <td><%= users[:fname] %></td>
          <td><%= users[:lname] %></td>
          <td><%= users[:email] %></td>
          <td>
          Currently disabled
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</main>